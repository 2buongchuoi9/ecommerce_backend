import { NotificationType } from "../helpers/constans.js"

const notificationService = {
    pushNotiSystem: async ({ type = NotificationType.ADMIN, receiveId, senderId, options = {} }) => {
        let noti_content
    },
}

export default notificationService
